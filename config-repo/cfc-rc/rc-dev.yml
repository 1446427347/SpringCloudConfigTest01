server:
  port: 9100
  tomcat:
    uri-encoding: UTF-8
    max-thread: 1000
spring:
  boot:
    admin:
      client:
        url: http://localhost:1000
#  zipkin:
#    base-url: http://localhost:9411
#  sleuth:
#    sampler:
#      # 采样率值介于0到1之间，1表示全部采集
#      probability: 1
 #关闭安全访问
management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
